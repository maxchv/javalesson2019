package ua.step.homework;

import java.io.IOException;

/**
 * Данны два числа x и y. Программа должна вывести в консоль «yes» если оба
 * числа четные, либо «no» оба числа не четные, иначе программа ничего не выводит.
 */
public class Task08 {
    public static void main(String[] args) {
        int x = 3;
        int y = 2;

        // строки ниже нужны для тестирования, не удаляйтее ее и не изменяйте
        x = (args.length > 0) ? Integer.valueOf(args[0]) : x;
        y = (args.length > 1) ? Integer.valueOf(args[1]) : y;

        // пишите код ниже испоьзуя переменные объявленные выше, проверить
        // решение можно запустив @see TestTask08.java

        // TODO: здесь ваш код


    }
}